import styled from 'styled-components';
import { Table, Button } from 'antd';

export const ConfirmUserTable = styled(Table)`
	margin-top: 30px;
`;

export const TableButtonWrapper = styled.div`
	display: flex;
	flex-wrap: wrap;
`;

export const TableButton = styled(Button)`
	margin-right: 10px;
`;
